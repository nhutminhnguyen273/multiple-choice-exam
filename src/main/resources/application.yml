server:
  port: 8080
  servlet:
    context-path: /multiple-choice-exam

spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/multiple_choice_exam"
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true